---
classes:
  - ubuntu_pkgs
  - vagrant
  - unix_base
  - stdlib
  - docker

docker::tcp_bind: tcp://0.0.0.0:2375
docker::version: 1.11.2-0~xenial
docker::package_name: docker-engine

vagrant::vagrant_user: ubuntu

nomadproject::version: 0.4.0

ubuntu_pkgs:
  - unzip
